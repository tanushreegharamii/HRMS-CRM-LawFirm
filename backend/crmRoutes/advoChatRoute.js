// const express = require("express");
// const router = express.Router();
// const { verifyToken } = require("../middlewares/generateToken");
// const {
//   initiateAdvoChat,
//   getAdvoMessages,
//   sendAdvoMessage,
// } = require("../crmControllers/advocateChatController");

// router.get("/:receiverId/initiate", verifyToken, initiateAdvoChat);
// router.get("/:advoChatRoomId/messages", verifyToken, getAdvoMessages);
// router.post("/:advoChatRoomId/send", verifyToken, sendAdvoMessage);

// module.exports = router;
